<% include ../partials/header %>

<div class="container">

  <a href="/condos/<%= condo_id %>/towers/<%= tower_id %>/apartments/<%= apartment_id %>/dwellers/<%= dweller._id %>/edit"><i class="fa fa-pencil" aria-hidden="true"></i></a>
  <form id="delete-from" action="/condos/<%= condo_id %>/towers/<%= tower_id %>/apartments/<%= apartment_id %>/dwellers/<%= dweller._id %>?_method=DELETE" method="post">
    <button type="submit" class="btn btn-danger">
      <i class="fa fa-trash" aria-hidden="true"></i>
    </button>
  </form>

    <h1><%= dweller.name %></h1>

    <h3>CPF: <%= dweller.cpf %></h3>

    <a href="#">Go Back</a>

</div>

<% include ../partials/footer %>